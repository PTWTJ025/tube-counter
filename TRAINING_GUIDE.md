# 📘 คู่มือโมเดล YOLO — ระบบนับหลอดด้าย

> **สำหรับ:** ภูตะวัน  
> **โมเดลที่ใช้:** `model/best.pt` (YOLOv8)  
> **เทรนผ่าน:** Roboflow  
> **อัปเดตล่าสุด:** กันยายน 2026

---

## 📌 ภาพรวมระบบ

`
ผู้ใช้ถ่ายรูป
    ↓
แอป (index.html) ส่งภาพไป POST /api/detect
    ↓
Server (Render) รัน YOLO model → นับ bounding box
    ↓
คืนค่า: จำนวนหลอด + ภาพที่วาด bbox แล้ว
    ↓
ผู้ใช้เห็นผลและสามารถแก้ไขก่อนบันทึก
`

---

## 🔢 ค่า Parameter ที่ใช้ตอน Inference (ใช้งานจริง)

ตั้งค่าใน `main.py` → `count_with_yolo()`

| Parameter | ค่าที่ใช้ | ช่วงปกติ | ความหมาย |
|-----------|----------|----------|----------|
| `conf` | **0.25** | 0.1 – 0.9 | ความมั่นใจขั้นต่ำที่ model ต้องมีถึงจะนับ detection นั้น |
| `iou`  | **0.45** | 0.1 – 0.9 | ถ้า bbox 2 อันซ้อนกันเกินนี้ → ตัดทิ้งอันที่ confidence ต่ำกว่า |
| `imgsz`| **1280** | 640/1280  | ขนาดภาพที่ resize ก่อนส่งเข้า model (px) |

---

## 🧠 อธิบายแต่ละค่าแบบละเอียด

### conf — Confidence Threshold

Model คิดว่า "นี่คือหลอด" ด้วยความมั่นใจ 80% → conf=0.25 → ✅ นับ  
Model คิดว่า "นี่คือหลอด" ด้วยความมั่นใจ 15% → conf=0.25 → ❌ ไม่นับ

| ค่า conf | ผล |
|----------|----|
| ต่ำ (0.1–0.2) | จับได้เยอะ แต่อาจนับของที่ไม่ใช่หลอดด้วย (False Positive) |
| **0.25** ← ใช้อยู่ | สมดุลระหว่างจับครบและจับถูก |
| สูง (0.5–0.8) | แม่นกว่า แต่อาจตกหล่นหลอดที่มองเห็นไม่ชัด (False Negative) |

> ถ้าโมเดลนับเยอะเกิน → เพิ่ม conf ขึ้น (เช่น 0.4)
> ถ้าโมเดลนับน้อยเกิน → ลด conf ลง (เช่น 0.15)

---

### iou — Intersection over Union (NMS Threshold)

ใช้ตัดกรณีที่ model วาด bbox ซ้อนกันหลายอันบนหลอดเดียวกัน

| ค่า iou | ผล |
|---------|----|
| ต่ำ (0.2–0.3) | ตัด bbox ที่ซ้อนกันออกเยอะ → นับได้น้อยลง |
| **0.45** ← ใช้อยู่ | มาตรฐาน YOLO ใช้ได้กับวัตถุส่วนใหญ่ |
| สูง (0.7–0.9) | ยอมให้ bbox ซ้อนกันมากขึ้น → อาจนับซ้ำ |

> ถ้า 1 หลอดถูกนับ 2 ครั้ง → ลด iou ลง (เช่น 0.3)
> ถ้าหลอดที่ชิดกันถูกตัดทิ้ง → เพิ่ม iou ขึ้น (เช่น 0.6)

---

### imgsz — Input Image Size

| ขนาด | ความเร็ว | ความแม่น |
|------|----------|----------|
| 640  | เร็ว | ปานกลาง |
| **1280** ← ใช้อยู่ | ช้ากว่า 2x | แม่นกว่า (เหมาะกับหลอดเล็กๆ จำนวนมาก) |

---

## 📊 ตัวชี้วัด (Metrics) หลัง Train

ดูใน Roboflow → Versions → [version ล่าสุด] → Metrics

| Metric | ความหมาย | เป้าหมาย |
|--------|----------|----------|
| **mAP50** | ความแม่นที่ IoU=0.5 | > 0.80 |
| **mAP50-95** | ความแม่นเฉลี่ยหลาย IoU | > 0.60 |
| **Precision** | ที่นับมาทั้งหมด ถูกกี่ % | > 0.75 |
| **Recall** | หลอดจริงๆ ถูกจับได้กี่ % | > 0.75 |

---

## 🔧 วิธีปรับหากผลไม่ดี

**นับมากเกิน:**
`conf=0.40, iou=0.35`

**นับน้อยเกิน:**
`conf=0.15, iou=0.55`

**โมเดลแย่ทั้งคู่ → เทรนใหม่:**
- เพิ่มภาพ training (ตอนนี้ ~94 ใบ → ควรเป็น 200+)
- เพิ่ม augmentation (ภาพมืด, มุมเอียง)
- เพิ่ม epochs

---

## 📁 โครงสร้างไฟล์

`
tube-counter/
├── model/best.pt       ← โมเดล YOLO (5.4 MB)
├── main.py             ← Backend (count_with_yolo)
├── requirements.txt    ← ultralytics, opencv
├── index.html          ← Frontend
└── Img/Heic to png/    ← ภาพ training (94 ใบ)
`

---

## 🔄 วิธี Update โมเดลใหม่

1. Download best.pt ใหม่จาก Roboflow
2. แทนที่ไฟล์ model/best.pt
3. `git add model/best.pt && git commit -m "update: new model" && git push`
4. Render redeploy อัตโนมัติ

---

## ❓ คำศัพท์

| คำ | ความหมาย |
|----|----------|
| YOLO | You Only Look Once — algorithm ตรวจจับวัตถุแบบเร็ว |
| Bounding Box | กรอบสี่เหลี่ยมที่วาดรอบวัตถุ |
| Confidence | ความมั่นใจของโมเดล (0–1) |
| IoU | วัดว่า bbox ซ้อนทับกันแค่ไหน (0–1) |
| NMS | Non-Maximum Suppression — ตัด bbox ซ้ำ |
| mAP | Mean Average Precision — ตัวชี้วัดรวม |
| Epoch | 1 รอบที่โมเดลเรียนรู้จากข้อมูลทั้งหมด |
| Inference | นำโมเดลมาใช้กับภาพจริง |
